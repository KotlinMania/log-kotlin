# Port targets — `log-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `log` that codex-rs uses, and therefore the minimum subset that `log-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-77] (rank `2`, tier `direct`, status `To Do`)

**Cargo package name**: `log`
**Rust module name (in `use` statements)**: `log`

## Summary

- **5** distinct symbols imported by codex-rs
- **5** total `use` statements
- **3** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `log::LevelFilter`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-state`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `state/src/runtime.rs:35` (`codex-state`) — `use log::LevelFilter`

### `log::LogEntry`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-state`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `state/src/model/mod.rs:19` (`codex-state`) — `use log::LogEntry`

### `log::LogQuery`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-state`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `state/src/model/mod.rs:20` (`codex-state`) — `use log::LogQuery`

### `log::LogRow`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-state`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `state/src/model/mod.rs:21` (`codex-state`) — `use log::LogRow`

### `log::info`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-execpolicy-legacy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `execpolicy-legacy/src/policy_parser.rs:8` (`codex-execpolicy-legacy`) — `use log::info`
